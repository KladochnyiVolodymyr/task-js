export default class AverageAge {
  constructor(arr) {
    this.arr = arr;
  }
  averageAge(arr) {
    let age = this.arr.filter(function(element) {
      return element.age;
    });
    let res = age.reduce(function() {
      
    });
  }
}
  
